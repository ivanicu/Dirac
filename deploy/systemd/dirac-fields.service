# dirac-fields.service — unified Dirac application and scientific service (:8901).
# Historical field and physics routes share this Command/Method/Job/Artifact control
# plane. The standalone :8902 unit is archived and must stay disabled.

[Unit]
Description=Dirac application and scientific service (:8901)
After=postgresql.service network.target
StartLimitIntervalSec=300
StartLimitBurst=5

[Service]
Type=simple
WorkingDirectory=/home/ivan/dirac
# Scratch goes to DISK, not to /tmp. Measured 2026-08-11: pyscf's cubegen writes the
# whole 6.7 MB cube into TMPDIR before returning it, and /tmp here is a 30 GB tmpfs with
# a PER-USER quota. With the quota tight, the same calculation dies with
# `OSError: [Errno 122] Disk quota exceeded` from inside cubegen.write — an error a
# chemist can do nothing with, arriving from a path that has nothing to do with them.
# backend/scratch.py sets this in code as well, so every entry point is covered; this
# line exists so an operator reading the unit can SEE where the scratch lives.
Environment=TMPDIR=/home/ivan/.cache/dirac/scratch
Environment=PYSCF_TMPDIR=/home/ivan/.cache/dirac/scratch
# GPU-class Motif methods cross the fenced Kubernetes/Kueue worker protocol.
# CPU methods remain local inside the bounded controller pool.
Environment=DIRAC_EXECUTOR=kubernetes
Environment=DIRAC_KUBERNETES_EXCHANGE_HOST=/home/ivan/dirac/.runtime/pv/exchange
# Optional operator-owned provider URLs, credentials and explicit shared-GPU grant.
# The ignored file is absent on hosts that intentionally run without AI inference.
EnvironmentFile=-/home/ivan/dirac/deploy/ai/dirac-ai.env
ExecStart=/home/ivan/dirac/backend/env/bin/python backend/field_server.py
Restart=on-failure
RestartSec=5

# Ceilings carried over unchanged from dirac-backend.service, where they were
# derived from measurement: pyscf plans around a 4000 MB max_memory per SCF
# object, and idle RSS was measured at ~160 MB (fields) and ~1.06 GB (physics).
# PER-UNIT now rather than shared, which is strictly safer: one runaway SCF can
# no longer consume the other daemon's headroom before either gets throttled.
MemoryHigh=6G
MemoryMax=8G

[Install]
WantedBy=default.target
